{
  "mcpServers": {
    "codemap": {
      "command": "codemap",
      "args": [
        "mcp",
        "--configured-version",
        "dev",
        "--integration",
        "codex-plugin"
      ]
    }
  }
}
